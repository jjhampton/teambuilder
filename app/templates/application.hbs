<nav class="primary-nav">
  {{#if session.isAuthenticated}}
    <span class="nav-buttons-authenticated">
      <span>Welcome, {{session.currentUser.firstName}} {{session.currentUser.lastName}}</span>
      {{#link-to 'index'}}
        <button class="nav-button-home">Home</button>
      {{/link-to}}
      {{#link-to 'users.show' session.currentUser}}
        <button class="nav-button-profile">Profile</button>
      {{/link-to}}
    </span>
    <button class="nav-button-logout" {{action 'invalidateSession'}}>Logout</button>
  {{else}}
    {{sessions-create}}
  {{/if}}
</nav>
{{outlet}}
